{% extends 'base.html.twig' %}

{% block title %}CategoryNFT{% endblock %}

{% block body %}
    <h1>CategoryNFT</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_n_f_t.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category_n_f_t.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ category_n_f_t.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_category_n_f_t_index') }}">back to list</a>

    <a href="{{ path('app_category_n_f_t_edit', {'id': category_n_f_t.id}) }}">edit</a>

    {{ include('category_nft/_delete_form.html.twig') }}
{% endblock %}
