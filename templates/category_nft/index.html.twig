{% extends 'base.html.twig' %}

{% block title %}CategoryNFT index{% endblock %}

{% block body %}
    <h1>CategoryNFT index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category_n_f_t in category_n_f_ts %}
            <tr>
                <td>{{ category_n_f_t.id }}</td>
                <td>{{ category_n_f_t.name }}</td>
                <td>{{ category_n_f_t.description }}</td>
                <td>
                    <a href="{{ path('app_category_n_f_t_show', {'id': category_n_f_t.id}) }}">show</a>
                    <a href="{{ path('app_category_n_f_t_edit', {'id': category_n_f_t.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_category_n_f_t_new') }}">Create new</a>
{% endblock %}
