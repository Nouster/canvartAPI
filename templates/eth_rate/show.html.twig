{% extends 'base.html.twig' %}

{% block title %}EthRate{% endblock %}

{% block body %}
    <h1>EthRate</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ eth_rate.id }}</td>
            </tr>
            <tr>
                <th>RateDate</th>
                <td>{{ eth_rate.rateDate ? eth_rate.rateDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ eth_rate.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_eth_rate_index') }}">back to list</a>

    <a href="{{ path('app_eth_rate_edit', {'id': eth_rate.id}) }}">edit</a>

    {{ include('eth_rate/_delete_form.html.twig') }}
{% endblock %}
