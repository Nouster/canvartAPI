{% extends 'base.html.twig' %}

{% block title %}CollectionNFT index{% endblock %}

{% block body %}
    <h1>CollectionNFT index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for collection_n_f_t in collection_n_f_ts %}
            <tr>
                <td>{{ collection_n_f_t.id }}</td>
                <td>{{ collection_n_f_t.name }}</td>
                <td>
                    <a href="{{ path('app_collection_n_f_t_show', {'id': collection_n_f_t.id}) }}">show</a>
                    <a href="{{ path('app_collection_n_f_t_edit', {'id': collection_n_f_t.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_collection_n_f_t_new') }}">Create new</a>
{% endblock %}
