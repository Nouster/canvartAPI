api_platform:
  security:
    authentication:
      custom_token_authenticator: App\Security\ApiTokenAuthenticator
  metadata_backward_compatibility_layer: false
  mapping:
    paths: ["%kernel.project_dir%/src/Entity"]
  patch_formats:
    json: ["application/merge-patch+json"]
  swagger:
    versions: [3]
